{
  "name": "two-inc/magento2-hyva-checkout",
  "description": "Two payment gateway for Magento 2 + Hyvä — JS dev-only manifest. The module's runtime metadata lives in composer.json; this file exists so prettier and the npm-based Jest test runner have somewhere to declare devDeps without polluting Composer or Packagist.",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "test:js": "jest --config Test/Js/jest.config.js"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.24.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.6.2"
  }
}
