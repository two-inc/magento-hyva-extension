<?php
/**
 * Hyvä Themes - https://hyva.io
 * Copyright © Hyvä Themes 2020-present. All rights reserved.
 * This product is licensed per Magento install
 * See https://hyva.io/license
 */

declare(strict_types=1);

use Hyva\Theme\Model\ViewModelRegistry;
use Hyva\Theme\ViewModel\HyvaCsp;
use Two\GatewayHyva\ViewModel\BrandedHyvaViewModelInterface;
use Two\GatewayHyva\ViewModel\CheckoutConfig;
use Two\GatewayHyva\ViewModel\GetQuoteDetails;
use Magento\Framework\Escaper;

/** @var \Magento\Framework\View\Element\Template $block */
/** @var ViewModelRegistry $viewModels */
/** @var HyvaCsp $hyvaCsp */
/** @var Escaper $escaper */

/** @var BrandedHyvaViewModelInterface $brandedViewModel */
$brandedViewModel = $block->getData('branded_view_model');
$gwBase = $brandedViewModel->getAlpineFnPrefix() . 'GatewayHyva';

$configModel = $viewModels->require(CheckoutConfig::class);
$quoteModel = $viewModels->require(GetQuoteDetails::class);
$hyvaCsp = $viewModels->require(HyvaCsp::class);

$checkoutApiUrl = $configModel->getCheckoutApiUrl();
$isAddressSearchEnabled = $configModel->getIsAddressSearchEnabled();
$isCompanySearchEnabled = $configModel->getIsCompanySearchEnabled();
$companySearchMinChars = $configModel->getCompanySearchMinChars();
$quoteDetails = $quoteModel->getQuoteDetails();
// Same flag set as gateway_method-csp-js.phtml, for the same reason: this
// lands in an inline script and carries buyer-supplied values (email,
// telephone, name, product names and descriptions). JSON_HEX_TAG is the
// load-bearing one — without it a product named with a script end tag closes
// the block early, which is both script injection and a second way to move
// where HyvaCsp thinks this element starts and ends. JSON_UNESCAPED_SLASHES
// is deliberately absent: it strips the incidental protection plain
// json_encode() gives by escaping the slash in an end tag.
// JSON_INVALID_UTF8_SUBSTITUTE and the `?: '{}'` are not belt-and-braces. A
// single malformed byte in a product name makes json_encode() return false, and
// this value is interpolated bare, so the template would emit `quote : ,` — a
// syntax error that kills every Alpine.data() registration in the block. That is
// this very outage arriving through data. Substitute the bad byte, and if the
// encode still fails fall back to an empty object: the country lookup then falls
// through to reading the DOM, which degrades instead of dying.
$quoteDetailsJson = json_encode(
    $quoteDetails,
    JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_QUOT | JSON_HEX_AMP
    | JSON_UNESCAPED_UNICODE | JSON_INVALID_UTF8_SUBSTITUTE,
) ?: '{}';

/** Generated with hyva-csp-helper.php */
?>
<script>
    /*
     * Resolved AT CALL TIME, not at script-eval time, and never by calling
     * `window.twoGatewayReadCompanySelection(...)` bare.
     *
     * Call-time matters: the accessors are published by
     * gateway_method-csp-js.phtml, and this template does not reliably render
     * after it. shipping_company.phtml goes into `head.additional` and so runs
     * BEFORE the publisher; resolving at eval time there would bind the fallback
     * permanently and silently disable carry-over. Deferring makes all three
     * consumers order-independent instead of two-of-three-happen-to-be-fine.
     *
     * The fallback matters because these call sites sit inside the `alpine:init`
     * / `DOMContentLoaded` handlers that also register the pickers and start the
     * payment-form observer: a bare call on a page without the publisher would
     * throw BEFORE those registrations and leave a dead checkout step.
     * Degrading to "no carry-over between steps" is survivable; that is not.
     *
     * Uniquely named per template because these share one global lexical scope,
     * so a name reused across two templates is a redeclaration SyntaxError that
     * kills whichever block is evaluated second.
     *
     * (Deliberately not writing the script start tag in that sentence: Hyva
     * picks the element to sign with a LAST-occurrence, case-insensitive search
     * for it, so a second literal anywhere in the file — a comment included —
     * leaves the real tag unsigned and the enforced checkout CSP refuses the
     * whole block. Test/Unit/View/CspInlineScriptTemplateTest guards it.)
     */
    const twoAddressReadSelection = function() {
        return (window.twoGatewayReadCompanySelection
            || function() { return {}; })();
    };
    const twoAddressWriteSelection = function(patch) {
        return (window.twoGatewayWriteCompanySelection
            || function() { return {}; })(patch);
    };

function <?= $gwBase ?>CompanySearchField() {
    return {
        isOpen: false,
        search: '',
        items: [],
        selectedIndex: -1,
        // True from the moment selectItem() runs until the debounced getItems()
        // tick behind its synthetic `input` finally fires with no REAL edit
        // having landed in between. Its only remaining job is telling that tick
        // apart from an ordinary one, so it must NOT re-search the name just
        // picked. It does not gate noteCompanyQuery() any more (see
        // `awaitingSelectionEcho`) — a real keystroke clears it early, in
        // noteCompanyQuery() itself, so a debounced tick that lands behind a
        // genuine correction searches for that correction instead of silently
        // no-opping.
        isSelecting: false,
        /*
         * One-shot, consumed by the very NEXT `noteCompanyQuery()` call —
         * always the synthetic `input` selectItem() dispatches on itself, since
         * that call happens synchronously inside selectItem() before control
         * returns to whatever triggered the selection (TWO-25288 element 5
         * round 2).
         *
         * `isSelecting` used to do this job too, but it lives across the whole
         * debounce window rather than one call, so a REAL keystroke typed
         * before the debounced getItems() tick fired was gated by the exact
         * same flag as the synthetic echo — both the manual-entry link and the
         * min-chars hint stayed frozen in the just-selected state for as long
         * as the buyer kept typing within 500ms of the pick, which is a worse
         * affordance than the one this ticket started from. Splitting the
         * "ignore my own echo" job onto a flag consumed by exactly one call
         * fixes that: only the echo itself is swallowed, and the very next
         * keystroke — echo or not — is free to update `isCompanySelected` and
         * the visible query on the spot.
         */
        awaitingSelectionEcho: false,
        // True from the moment a company is chosen from the dropdown until the
        // buyer edits the field again. Distinct from `isSelecting`, which is a
        // one-shot flag consumed by the very next getItems() tick: this one has
        // to OUTLIVE that tick, because `selectItem()` writes the chosen name
        // into `search` and a company whose name is shorter than the threshold
        // would otherwise leave the min-characters hint under a populated,
        // closed field.
        isCompanySelected: false,
        manualMode: false,
        /*
         * The company the identifier below currently belongs to, and that
         * identifier.
         *
         * `companyName` is NOT the live field text — `search` is. It records
         * the name a registry-supplied identifier was written FOR, so the
         * editability rule can ask whether the text still describes the same
         * company. Collapsing the two would make every keystroke look like it
         * still had a vouched-for number.
         */
        companyName: '',
        companyId: '',
        /*
         * Where the identifier in `companyId` came from: `'registry'` for one a
         * search hit supplied, `'manual'` for one the buyer typed, `''` for
         * none.
         *
         * Load-bearing because both values live under the same storage key, and
         * the two have opposite editability rules — a registry number must not
         * be typed over, a hand-typed one must always be correctable. Without
         * the provenance the restore path could only ask "is there a number",
         * so the first Magewire re-render after the buyer typed one locked the
         * field over their own value and a typo could never be fixed.
         *
         * It is also what makes the stale-identifier clear below safe: only a
         * registry number is dropped when the name stops matching, because only
         * a registry number belongs to a name at all.
         */
        companyIdSource: '',
        /*
         * Bound by companyName.phtml as `:disabled` on the company-number
         * input, and the ONLY thing that locks that field — the greyed look
         * derives from it in custom.css (`input.company_id:disabled`), shared
         * with the payment tile.
         *
         * Declared locked so the field cannot flash open before init() runs.
         */
        companyIdDisabled: true,
        /*
         * Set whenever there is no registry-supplied identifier for the name
         * the field currently holds: the selected company arrived without a
         * national identifier (the search response is allowed to omit it), the
         * buyer typed or edited the name without picking a hit, or manual
         * entry is active. The number is what placement credit-checks, so the
         * buyer has to be able to type it in themselves in every one of those
         * cases — otherwise the field sits empty AND locked, and there is no
         * other company-number input in the checkout to fall back on.
         */
        companyIdEntryRequired: false,
        checkoutApiUrl: '<?= $escaper->escapeJs($checkoutApiUrl) ?>',
        isAddressSearchEnabled: '<?= $escaper->escapeJs((string) $isAddressSearchEnabled) ?>',
        /*
         * Whether the registry lookup is switched on for this store.
         *
         * With it off this field pair is a plain name + number form: no
         * request, no dropdown, neither mode link, and the number always
         * typeable — which is why the editability rule below carries a term
         * for it rather than the template carrying a PHP branch.
         */
        isCompanySearchEnabled: '<?= $escaper->escapeJs((string) $isCompanySearchEnabled) ?>',
        quoteData: <?= $quoteDetailsJson ?>,
        // The one threshold for this repo, emitted from PHP
        // (CheckoutConfig::COMPANY_SEARCH_MIN_CHARS) rather than written as a
        // literal here — the guard below and the hint that claims the number
        // must be unable to disagree (TWO-25288). Cast to an int so the
        // comparison stays numeric; a quoted value would compare a string.
        minSearchChars: <?= (int) $companySearchMinChars ?>,
        // Placeholder copy (TWO-25288). Held as state, not written inline in
        // the markup only, because the manual-entry mode has to be able to
        // blank it: "Enter company name to search" over a field the buyer is
        // typing into by hand is wrong.
        companyNamePlaceholder: '<?= $escaper->escapeJs(__('Enter company name to search')) ?>',
        countrySelectionShown: false,
        // Company-search request state. `isSearching` drives an in-field
        // spinner and `isSearchUnavailable` a "try again / enter manually"
        // notice, so a failed search is never rendered as a genuine
        // zero-result one. NOTE: the markup that binds these lives in
        // companyName.phtml, which is being changed concurrently by the
        // CSP-Alpine work — the state lands here now and the template chrome
        // follows in that PR rather than colliding with it.
        isSearching: false,
        isSearchUnavailable: false,
        searchAbortController: null,

        init() {
            // Restore manualMode from storage
            const data = twoAddressReadSelection();
            this.manualMode = data.manual_mode || false;

            /*
             * Restore the company pair too, so the number the buyer typed on a
             * previous page load — or the one a registry pick supplied —
             * survives the Magewire re-renders that destroy and rebuild this
             * component. Only selectItem() and onCompanyIdInput() write
             * storage, so an unrestored component would come back empty and
             * the checkout would lose the identifier placement needs.
             *
             * `companyName` is restored alongside the id deliberately: the
             * editability rule compares the field text against it, and
             * restoring one without the other would make a restored pick look
             * like an edited name and unlock a registry-supplied number.
             *
             * The provenance is restored with them and the lock derives from it,
             * NOT from "is there a number". Both a registry pick and a
             * hand-typed number land under the same key, so an unqualified
             * `!this.companyId` re-locked the field over the buyer's own value
             * on the next re-render and made a typo uncorrectable. An absent or
             * unrecognised source leaves the field typeable: nothing has vouched
             * for that number, and the failure that direction is recoverable
             * while the other one is a dead end.
             */
            this.companyName = data.company_name || '';
            this.companyId = data.company_id || '';
            this.companyIdSource = data.company_id_source || '';
            this.companyIdEntryRequired = !this.hasVouchedCompanyId();
            this.applyCompanyIdEditability();
            /*
             * A restored pick with a valid identifier is a completed selection,
             * exactly like one made this page load via selectItem() (TWO-25288
             * element 5 round 2). Without this, `isCompanySelected` stayed at
             * its `false` default across a reload, and `persistentManualEntryVisible`
             * showed "my company is not on the list" beside a field that
             * already held a valid, restored answer.
             *
             * Keyed off `companyId` alone, not `hasVouchedCompanyId()`: a
             * restored MANUAL identifier is just as complete a pick as a
             * registry one, and `hasVouchedCompanyId()` answers a narrower
             * question — whether the number may still be edited — not whether
             * a company has been chosen.
             */
            this.isCompanySelected = Boolean(this.companyId);
        },

        /*
         * `items.length > 0` is deliberately NOT a term any more (TWO-25288
         * element 5).
         *
         * The manual-entry row is this dropdown's last row, and it has to be
         * there exactly when a buyer cannot find their company: a search that
         * matched nothing, one that failed, or one that has not come back yet.
         * Gating the panel on a non-empty result set made the row unreachable
         * in precisely those cases — the only cases it exists for.
         *
         * `searchModeActive` carries the manual-mode and lookup-disabled terms,
         * so the panel still cannot open over the manual-entry fields or on a
         * store with the lookup switched off. That is a term the old expression
         * did NOT have, and the reason it was safe without one was the results
         * gate now being removed.
         */
        showDropdown() {
            return this.isOpen && this.searchModeActive;
        },

        /**
         * Undebounced `input` handler, running alongside the debounced
         * getItems() (TWO-25288 element 5).
         *
         * The manual-entry row has to be reachable as soon as the buyer has
         * typed enough characters to search — BEFORE the debounce elapses and
         * before any result exists. getItems() was the only thing that opened
         * the dropdown, and only ever on a non-empty result set, so the row
         * could not appear until a search had both fired and matched.
         *
         * Bound as a SECOND `@input` attribute rather than folded into
         * getItems(): `@input` and `@input.debounce.500ms` are distinct
         * attribute names, so Alpine registers both listeners, and the debounce
         * has to stay on the request half — an undebounced search would put one
         * request per keystroke on the wire.
         *
         * `awaitingSelectionEcho` is the one this swallows, and only for the
         * single call it names: selectItem() sets it and then dispatches
         * `input` on the field, so this handler runs first, synchronously,
         * before selectItem() returns. Consuming it here — not leaving it for
         * getItems() the way `isSelecting` is — is what stops a REAL keystroke
         * typed a moment later from being mistaken for that same echo (TWO-25288
         * element 5 round 2): the echo is the only call gated, so the very next
         * one always reaches the code below.
         *
         * `isSelecting` is read but deliberately CLEARED here rather than left
         * alone, on any call that gets this far — i.e. every call except the
         * echo itself. That is what makes a real correction typed within the
         * debounce window reach the search: without it, the debounced
         * getItems() tick behind this keystroke would still see `isSelecting`
         * true, hit its own one-shot guard, and silently skip searching for
         * the very text the buyer just corrected.
         *
         * Nothing is aborted, cleared or requested here beyond that. The
         * debounced handler owns the whole request lifecycle, and duplicating
         * any part of it is how `isSearching` ends up latched on.
         */
        noteCompanyQuery() {
            if (this.awaitingSelectionEcho) {
                this.awaitingSelectionEcho = false;
                return;
            }

            this.isSelecting = false;
            this.search = this.$el.value;
            // Any edit ends the selection, exactly as in getItems(). Written
            // here as well so the min-characters hint answers the keystroke
            // rather than the debounce half a second behind it.
            this.isCompanySelected = false;

            if (!this.searchModeActive) return;

            /*
             * Length alone, with an empty result list. Closing again below the
             * threshold is deliberate rather than a one-way latch: the
             * min-characters hint owns that range, and an empty panel floating
             * under a two-character query reads as a search that came back with
             * nothing.
             */
            this.isOpen = this.search.length >= this.minSearchChars;
        },

        /*
         * Mode-link visibility. Bare getters because CSP-friendly Alpine looks
         * the whole attribute string up as a key on the component rather than
         * evaluating it, so `!manualMode` and `manualMode && flag` are both
         * things the template cannot express.
         */
        get manualModeActive() {
            return Boolean(this.isCompanySearchEnabled) && this.manualMode;
        },

        get searchModeActive() {
            return Boolean(this.isCompanySearchEnabled) && !this.manualMode;
        },

        /*
         * Visibility of the manual-entry link that sits BELOW the field, as
         * opposed to the row inside the dropdown (TWO-25288 element 5).
         *
         * The two carry the same wording and perform the same action, so they
         * are never on screen together. What this link owns is "the panel is
         * SHUT" — an untouched field and a query too short to search, but also a
         * full-length query whose panel a click outside dismissed, because
         * closeDropdown() lowers `isOpen` without touching `search`. The row
         * owns the states in which the panel is open. Both are needed: an
         * untouched field is not reachable from the row's affordance.
         *
         * The two do NOT cover `searchModeActive` between them, and the third
         * term is why. The copy is a factual claim about the current state
         * rather than a label, so a completed selection has to offer neither:
         * the panel is shut and the query is at full length, so without
         * `isCompanySelected` this link told a buyer their company was not on
         * the list immediately after they picked it off that list. Not a dead
         * end — editing the name clears the flag in noteCompanyQuery(), on the
         * keystroke rather than half a second behind it, and the link returns to
         * the state the copy is true for again. (Manual entry
         * already being in effect offers neither too, via `searchModeActive`,
         * where the reverse link takes over.)
         *
         * `showDropdown` is called rather than re-deriving its terms, so this
         * cannot drift away from what the panel actually does.
         */
        get persistentManualEntryVisible() {
            return this.searchModeActive
                && !this.showDropdown()
                && !this.isCompanySelected;
        },

        /**
         * Is there an identifier that something other than the buyer vouched
         * for? Only a registry hit can; a hand-typed number is the buyer's own
         * and stays theirs to correct.
         *
         * @returns {boolean}
         */
        hasVouchedCompanyId() {
            return Boolean(this.companyId) && this.companyIdSource === 'registry';
        },

        /**
         * Drop a registry-supplied identifier that no longer belongs to the name
         * in the field, from component state AND from storage together.
         *
         * The two have to move as one. The blob is what the payment step reads,
         * and it derives its own editability from the presence of `company_id`
         * there — so a name written beside a previous company's number produced a
         * payment step showing the new name with the old number, LOCKED, with
         * nothing forcing the buyer to touch it, and sent the old organisation
         * number onward. Clearing state alone would only fix the address step's
         * display and leave the wrong number in the blob.
         *
         * Scoped to registry-supplied numbers on purpose: clearing a hand-typed
         * one here would delete the buyer's own entry every time they went back
         * and adjusted the name.
         */
        forgetStaleCompanyId() {
            if (!this.hasVouchedCompanyId()) return;
            if (this.search === this.companyName) return;

            this.companyId = '';
            this.companyIdSource = '';
            /*
             * Set here, not left to the caller. Dropping the number makes typing
             * one mandatory by definition, and the manual-mode caller below does
             * not recompute this flag — so without it, leaving manual mode
             * re-locked a field this had just emptied, which is the empty AND
             * locked AND required dead end.
             */
            this.companyIdEntryRequired = true;
            // A merging write, never a rebuild from a key list: dropping
            // `quote_id` that way is what silently disarmed the new-order clear.
            twoAddressWriteSelection({ company_id: '', company_id_source: '' });
        },

        /**
         * Recompute the company-number field's locked state.
         *
         * One derived state, one writer — the same shape as the payment tile's,
         * because the two fields mean the same thing and drifting apart is how
         * one of them ends up empty, required and uneditable.
         *
         * Locked exactly when a registry-supplied identifier has been written
         * for the name currently in the field. Unlocked otherwise, and
         * unconditionally unlocked on a store with the lookup off, where
         * nothing can ever vouch for a number.
         */
        applyCompanyIdEditability() {
            this.companyIdDisabled = !(
                !this.isCompanySearchEnabled
                || this.manualMode
                || this.companyIdEntryRequired
            );
        },

        /**
         * The buyer typed into the company-number field.
         *
         * Debounced by the template. Writes through the store-view-keyed
         * accessor and re-announces the pair on the same event a registry pick
         * uses, so the payment step sees a hand-typed identifier by exactly the
         * route it already sees a searched one — there is no second path.
         *
         * No name is written here and nothing is cleared: an identifier edit
         * says nothing about the name. Clearing the identifier when the NAME
         * changes is forgetStaleCompanyId()'s job, and only for registry ones.
         *
         * The provenance is written WITH the value, not derived from it later.
         * Both surfaces read one key, so without it the restore path cannot tell
         * this number from a registry pick's and locks the field over the
         * buyer's own value on the next re-render — at which point a typo is
         * uncorrectable, because this field is the only company-number input on
         * the address step.
         */
        onCompanyIdInput() {
            const value = this.$el.value;
            if (value === this.companyId) return;

            this.companyId = value;
            this.companyIdSource = 'manual';
            // A merging write, never a rebuild from a key list: dropping
            // `quote_id` that way is what silently disarmed the new-order clear.
            const companyData = twoAddressWriteSelection({
                company_id: value,
                company_id_source: 'manual'
            });

            window.dispatchEvent(new CustomEvent("shipping-company-selected", {
                detail: companyData
            }));
        },

        /**
         * Bound as `:placeholder` (TWO-25288). The markup also carries a static
         * `placeholder` attribute with the same source string, so the hint is
         * present in the server-rendered HTML before Alpine boots; this getter
         * is what lets manual mode clear it afterwards.
         */
        <?= $gwBase ?>GetCompanyNamePlaceholder() {
            return this.manualMode ? '' : this.companyNamePlaceholder;
        },

        /**
         * Bound as `x-show` on the min-characters hint (TWO-25288).
         *
         * Below the threshold getItems() returns early and this surface used to
         * render nothing at all, so a buyer typing one or two characters got
         * silence and no way to tell a too-short query from a broken search.
         *
         * NOT rendered at zero length: an empty field is the placeholder's job,
         * and stacking both reads as an error on a field nobody has touched.
         * Suppressed in manual mode, where no search runs at all, and after a
         * selection: a chosen company whose registered name is shorter than the
         * threshold is a complete, valid answer, so instructing the buyer to
         * type more over a field they have just finished with is wrong.
         *
         * A bare method rather than an inline expression because Hyvä's
         * CSP-friendly Alpine build resolves an attribute expression as a
         * property lookup and cannot evaluate operators.
         */
        <?= $gwBase ?>ShouldShowMinCharsMessage() {
            return !this.manualMode
                && !this.isCompanySelected
                && this.search.length > 0
                && this.search.length < this.minSearchChars;
        },

        // Bound as the dropdown's `x-for :key`, so it must be UNIQUE per row.
        // `companyId` is '' for every hit whose national identifier the search
        // response omitted — which it is allowed to do — so two such hits in
        // one response collided on '', and Alpine renders one row per distinct
        // key: the buyer lost a company that matched, term-dependently. Fall
        // back to the list position, unique by construction, prefixed so it can
        // never collide with a real identifier either.
        //
        // Mirrors <?= $gwBase ?>GetCompanyId() on the payment tile. That surface
        // was fixed on its own; this one is the same bug in the same shape.
        <?= $gwBase ?>GetCompanyId() {
            return this.item.companyId || `two-idx-${this.index}`;
        },

        /** Cancel any in-flight company search. Safe to call repeatedly. */
        abortCompanySearch() {
            if (this.searchAbortController) {
                this.searchAbortController.abort();
                this.searchAbortController = null;
            }
            this.isSearching = false;
        },

        /**
         * Record the typed company name when it no longer describes the
         * company a stored identifier was written for.
         *
         * Deliberately does NOT announce itself on `shipping-company-selected`.
         * The name is now written with no identifier beside it — the callers run
         * forgetStaleCompanyId() first, which drops a registry number the name no
         * longer describes — so announcing here would arm an order intent for a
         * company with no number at all. The identifier handler announces
         * instead, by which point the name in the blob is already this one.
         *
         * Nothing is written while the text still matches, so an intact
         * registry pick is never overwritten by its own field.
         *
         * `companyName` is deliberately NOT reassigned here. It records the
         * name the current identifier belongs to, and moving it to the typed
         * text would make the editability comparison match again on the very
         * next keystroke — re-locking a number field the edit had just
         * unlocked, one character later.
         */
        commitCompanyName() {
            if (this.search === this.companyName) return;

            // A merging write, never a rebuild from a key list: dropping
            // `quote_id` that way is what silently disarmed the new-order clear.
            twoAddressWriteSelection({ company_name: this.search });
        },

        async getItems() {
            // Every early return must leave the spinner down and nothing on
            // the wire, or `isSearching` latches true.
            if (this.isSelecting) {
                this.isSelecting = false;
                // Settled either way by the time this tick runs: the echo
                // already consumed it if it ran first, as it does in
                // production. Clearing it here too is what stops it being
                // mistaken for a still-pending echo by whatever
                // `noteCompanyQuery()` call comes next, in callers that reach
                // this tick without one ever having run.
                this.awaitingSelectionEcho = false;
                this.abortCompanySearch();
                return;
            }

            /*
             * Read the field BEFORE the mode guards, not after. Two independent
             * reasons, both load-bearing:
             *
             * The manual-mode guard used to return first, so in manual mode —
             * the only route on this surface for a company the registry has no
             * identifier for — the component never learned the name the buyer
             * had typed, and nothing recorded it. Placement needs a name
             * alongside the number, so that name has to be captured on the
             * manual path too.
             *
             * `search` is also what the min-characters hint measures, so leaving
             * it stale while in manual mode meant a field holding hand-typed
             * text carried whatever the last search was: switching back with
             * enableSearch() then showed no hint and ran no search until the
             * next keystroke. The `!manualMode` term on the hint is what keeps
             * it suppressed while manual entry is actually in effect.
             */
            this.search = this.$el.value;
            // Any edit ends the selection: the value in the field is a query
            // again, not a chosen company.
            this.isCompanySelected = false;

            if (this.manualMode || !this.isCompanySearchEnabled) {
                this.abortCompanySearch();
                // Same stale-pair clear as the search path below. A buyer who
                // picked a company and then switched to manual entry is editing
                // the name of a DIFFERENT company, so the picked number must not
                // travel with the new name either.
                this.forgetStaleCompanyId();
                this.commitCompanyName();
                this.applyCompanyIdEditability();
                return;
            }

            this.selectedIndex = -1;
            /*
             * The buyer has EDITED the name, so re-derive whether they have to
             * be able to type the number in themselves. An identifier belongs
             * to the name it arrived with: unless the text still matches the
             * name the current number was written for, nothing has vouched for
             * a number for what is in the field now.
             *
             * Deliberately not a blanket unlock — while the text still matches,
             * the field stays locked, which is what stops a registry-supplied
             * number being typed over.
             *
             * The clear runs FIRST, so the flag is computed against the pair as
             * it now stands rather than against the number just dropped.
             */
            this.forgetStaleCompanyId();
            this.companyIdEntryRequired =
                !(this.hasVouchedCompanyId() && this.search === this.companyName);
            this.applyCompanyIdEditability();
            this.isSearchUnavailable = false;
            if (this.search.length < this.minSearchChars) {
                this.abortCompanySearch();
                this.items = [];
                return;
            }
            /*
             * Recorded only once the text is long enough to be a company name —
             * BELOW the guard, not above it.
             *
             * The payment step reads this key, so committing above the guard
             * published every keystroke fragment as the company name, the empty
             * string included: a buyer who cleared the field left `company_name`
             * empty in the blob and the payment step showed no company at all.
             * The manual and lookup-off paths above commit unguarded on purpose —
             * there is no search there for a length to be a threshold FOR, and
             * the name the buyer types is the only one that will ever exist.
             */
            this.commitCompanyName();

            // Get country code using centralized helper (defined in gateway_method-csp-js.phtml)
            const countryCode = window.twoGatewayGetCountryCode(this.quoteData);

            // If no country available from any source, show a friendly message once
            if (!countryCode) {
                if (!this.countrySelectionShown) {
                    this.countrySelectionShown = true;
                    window.dispatchMessages([{
                        type: 'warning',
                        text: '<?= $escaper->escapeJs(__('Please select a country first')) ?>'
                    }], 5000);
                }
                this.abortCompanySearch();
                return;
            }
            this.countrySelectionShown = false;

            // Supersede any in-flight search so a slow response for an
            // earlier query cannot repopulate the dropdown.
            this.abortCompanySearch();
            const controller = new AbortController();
            this.searchAbortController = controller;
            this.isSearching = true;

            // try/catch: the helper is a window global, and an absent one
            // would throw out of getItems as an unhandled rejection with
            // `isSearching` latched true. Request failures are handled inside
            // the helper, so the catch means something structural.
            let result;
            try {
                result = await window.twoGatewayCompanySearch({
                    checkoutApiUrl: this.checkoutApiUrl,
                    countryCode: countryCode,
                    query: this.search,
                    limit: 10,
                    signal: controller.signal,
                });
            } catch (error) {
                console.error('Company search failed:', error);
                result = { status: 'failed', items: [] };
            }

            // The buyer typed on, or this component was torn down, while the
            // request was in flight. Writing anything now would fight the
            // newer search — stay silent.
            if (result.status === 'aborted' || this.searchAbortController !== controller) return;
            // enterManually() may have fired while this was in flight. Writing
            // `items` now would leave a populated result list behind the
            // manual-entry fields, ready to appear the moment the buyer goes
            // back to searching — and `isOpen` would still be set from the
            // keystroke that started this search. showDropdown() does now carry
            // a manual-mode term, so nothing is rendered over those fields, but
            // the stale list is still wrong to keep.
            if (this.manualMode) {
                this.isSearching = false;
                this.searchAbortController = null;
                return;
            }
            // Magewire may have re-rendered the address form while this was in
            // flight and replaced the node this component is bound to. Writing
            // `items` then populates a component that is no longer in the
            // document: no dropdown appears, and this dead copy is left holding
            // search state.
            //
            // Checked here rather than by putting `wire:ignore` on the x-data
            // node in companyName.phtml, which is the payment form's remedy for
            // the same class of problem. That subtree is not equivalent: the
            // payment form documents having no wire:model inputs inside it,
            // whereas this one wraps a Magewire-bound address field rendered by
            // Hyva's own form renderer, and opting it out of the diff-merge
            // cycle would stop the field updating.
            // Fails CLOSED: once Alpine has torn the subtree down there is no
            // `_x_dataStack` to walk up to, so `$root` is undefined — the state
            // this most needs to catch.
            if (!this.$root || !this.$root.isConnected) {
                this.isSearching = false;
                this.searchAbortController = null;
                return;
            }

            this.searchAbortController = null;
            this.isSearching = false;
            this.items = result.items;
            // A timeout, non-2xx, network error or degraded 200 must not be
            // rendered as "no companies found".
            this.isSearchUnavailable = result.status === 'failed' || result.status === 'degraded';
            if (this.items.length > 0) this.isOpen = true;
        },

        handleItemClick(event) {
            // Stop all event propagation to prevent address book modal from closing
            event.stopPropagation();
            event.stopImmediatePropagation();
            if (event.preventDefault) event.preventDefault();

            const index = event.currentTarget.dataset.index;
            if (index !== undefined && this.items[index]) {
                this.selectItem(this.items[index]);
            }
        },

        selectItem(item) {
            // The buyer has chosen; anything still on the wire is stale and
            // would reopen a dropdown they have just dismissed.
            this.abortCompanySearch();
            this.isSearchUnavailable = false;
            this.isSelecting = true;
            // Armed together: this one is for noteCompanyQuery()'s single echo
            // call below, `isSelecting` is for getItems()'s tick behind it.
            this.awaitingSelectionEcho = true;
            this.isOpen = false;
            this.items = [];
            this.search = item.companyName;
            this.isCompanySelected = true;
            this.manualMode = false;

            /*
             * The hit may carry no national identifier — the search response is
             * allowed to omit it — so record the pair as it arrived and derive
             * the lock from it. Written BEFORE the storage write and the event
             * below, so both carry the identifier this pick actually has rather
             * than the previous pick's.
             */
            this.companyName = item.companyName;
            this.companyId = item.companyId || '';
            // The one place that may claim registry provenance, and only when the
            // hit actually carried an identifier — a hit that arrived without one
            // has vouched for nothing.
            this.companyIdSource = this.companyId ? 'registry' : '';
            this.companyIdEntryRequired = !this.hasVouchedCompanyId();
            this.applyCompanyIdEditability();

            const input = this.$root.querySelector('input[type="text"]');
            if (input) {
                input.value = item.companyName;
                // Use bubbles: false to prevent address book modal from closing
                input.dispatchEvent(new Event('input', { bubbles: false }));
            }

            // A merging write, never a rebuild from a key list: dropping
            // `quote_id` that way is what silently disarmed the new-order clear.
            const companyData = twoAddressWriteSelection({
                company_name: item.companyName,
                company_id: item.companyId,
                company_id_source: this.companyIdSource,
                manual_mode: false
            });

            window.dispatchEvent(new CustomEvent("shipping-company-selected", {
                detail: companyData
            }));

            // Address lookup if enabled
            if (this.isAddressSearchEnabled && item.lookupId) {
                this.addressLookup(item.lookupId);
            }
        },

        async addressLookup(lookupId) {
            // Capture root element before async call since $root may not be available in callbacks
            const rootEl = this.$root;
            // Shared helper: carries the same 30s timeout as the search and
            // checks response.ok. Without that check an error body parsed as
            // an address payload and silently produced "no addresses".
            const data = await window.twoGatewayCompanyDetail(this.checkoutApiUrl, lookupId);
            if (data && data.addresses && data.addresses.length > 0) {
                this.setAddressData(data.addresses[0], rootEl);
            }
        },

        setAddressData(address, rootEl) {
            // Find the address section containing this company field
            // Traverse up levels from the component root to find the container with all address inputs
            let container = rootEl;
            for (let i = 0; i < 4 && container; i++) {
                container = container.parentElement;
            }

            if (!container) {
                console.warn('Could not find address container');
                return;
            }

            const cityInput = container.querySelector('input[name="city"]');
            const postcodeInput = container.querySelector('input[name="postcode"]');
            const streetInput = container.querySelector('input[name="street[0]"]');

            if (cityInput) {
                cityInput.value = address.city || "";
            }

            if (postcodeInput) {
                postcodeInput.value = address.postal_code || "";
            }

            if (streetInput) {
                streetInput.value = address.street_address || "";
            }

            // Trigger 'input' event with bubbles: false to prevent modal closure
            [cityInput, postcodeInput, streetInput].forEach(input => {
                if (input) {
                    input.dispatchEvent(new Event('input', { bubbles: false }));
                }
            });
        },

        enterManually(event) {
            // Stop event propagation to prevent address book modal from closing
            if (event) {
                event.stopPropagation();
                event.stopImmediatePropagation();
            }

            this.abortCompanySearch();
            this.isSearchUnavailable = false;
            this.manualMode = true;
            this.isOpen = false;
            this.items = [];
            this.saveManualModeToStorage(true);
            // Manual entry is one of the reasons the number field has to be
            // typeable; no separate unlock, the same single writer decides.
            this.applyCompanyIdEditability();
            const input = this.$root.querySelector('input[type="text"]');
            if (input) input.focus();
        },

        enableSearch(event) {
            // Stop event propagation to prevent address book modal from closing
            if (event) {
                event.stopPropagation();
                event.stopImmediatePropagation();
            }

            this.manualMode = false;
            this.saveManualModeToStorage(false);
            // Goes through the single writer rather than assigning a lock
            // directly: leaving manual mode must not re-lock a number the buyer
            // still has to fill in, because the name in the field has no
            // registry-supplied identifier either way.
            this.applyCompanyIdEditability();
            const input = this.$root.querySelector('input[type="text"]');
            if (input) input.focus();
        },

        saveManualModeToStorage(manualMode) {
            twoAddressWriteSelection({ manual_mode: manualMode });
        },

        closeDropdown() {
            this.abortCompanySearch();
            this.isSearchUnavailable = false;
            this.isOpen = false;
            this.items = [];
        },
    }
}
document.addEventListener('alpine:init', () => {
    Alpine.data('<?= $gwBase ?>CompanySearchField', <?= $gwBase ?>CompanySearchField);
});
</script>
<?php $hyvaCsp->registerInlineScript(); ?>
